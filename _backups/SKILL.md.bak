# 🎯 NeuroWwise 技能定义

## 概述

NeuroWwise 是一套 Wwise 音频工程 AI 辅助工具链，具备以下核心能力：

1. **逆向工程** - 从 Wwise 工程文件提取 DSL 代码
2. **数据增强** - 构建高质量训练数据集
3. **模型训练** - 微调专业 Wwise DSL 生成模型

---

## 工具技能详解

### 1. DSL 解析器 (V7.3)

**功能**: 核心语法解析引擎

**输入**: DSL 代码字符串

**输出**: 解析后的 AST 结构

**支持语法**:
```
Audio("name") { ... }
Container("name") { ... }
Sound("name", "path.wav") { ... }
Attenuation("name") { curve(...) }
Event("name") { actions }
SwitchGroup("name") { switches }
StateGroup("name") { states }
GameParameter("name", min, max, default)
```

**特性**:
- 嵌套结构解析
- Attenuation 曲线支持
- 属性键值对解析
- 错误位置定位

---

### 2. DSL 验证器 (V2.0)

**功能**: 验证 DSL 结构完整性

**输入**: DSL 代码 + 解析结果

**输出**: 验证报告 (通过/失败 + 错误详情)

**验证项目**:
- 括号匹配
- 必需属性检查
- 类型约束验证
- 引用完整性

---

### 3. XML to DSL 转译器 (V3.4)

**功能**: Wwise 工程逆向

**输入**: .wwu 文件路径

**输出**: DSL 代码

**支持对象**:
| Wwise 类型 | DSL 输出 |
|-----------|----------|
| ActorMixer | Audio |
| RandomSequenceContainer | Container(mode="random") |
| SwitchContainer | Container(mode="switch") |
| BlendContainer | Container(mode="blend") |
| Sound | Sound |
| Attenuation | Attenuation + curve() |
| Event | Event |
| SwitchGroup | SwitchGroup |
| StateGroup | StateGroup |
| GameParameter | GameParameter |

**特性**:
- 自动识别层级结构
- 保留属性配置
- 曲线点位提取
- RTPC 关联解析

---

### 4. 指令生成器 (V1.1)

**功能**: 为 DSL 样本生成自然语言指令

**输入**: DSL 代码

**输出**: instruction + input + output 格式

**指令类型**:
```
- "创建一个..."
- "配置...的衰减效果"
- "设置...的RTPC参数"
- "生成完整的...工作流"
```

**特性**:
- 多样化指令模板
- 上下文感知
- 中英文混合

---

### 5. 样本裂变器 (V1.1)

**功能**: 数据增强

**输入**: 原始样本

**输出**: 增强后的多个变体

**增强策略**:
- 参数变体 (数值调整)
- 名称替换 (同类名称)
- 结构变体 (子元素增减)
- 指令改写 (同义表达)

**增强倍率**: 2-5x

---

### 6. 数据集优化器 (V1.0)

**功能**: 平衡样本类型分布

**输入**: 原始数据集

**输出**: 平衡后的数据集

**优化策略**:
- 过采样少数类
- 欠采样多数类
- 类型比例调整

**目标分布**:
```
Audio: 25%
Attenuation: 15%
Event: 15%
Workflow: 15%
SwitchGroup: 10%
StateGroup: 10%
GameParameter: 10%
```

---

### 7. 数据集分析器 (V1.0)

**功能**: 数据集质量分析

**输入**: JSONL 数据集

**输出**: 分析报告

**分析维度**:
- 样本总数
- 类型分布
- Token 长度分布
- 质量评分
- 异常检测

---

### 8. HuggingFace 上传器 (V1.0)

**功能**: 上传数据集到 HuggingFace

**输入**: JSONL 文件 + HF Token

**输出**: HuggingFace 数据集链接

---

### 9. Colab 训练脚本 (V2.0)

**功能**: 云端模型微调

**输入**: HuggingFace 数据集

**输出**: LoRA adapter + GGUF 模型

**技术栈**:
- Unsloth (高效微调)
- QLoRA (4-bit 量化)
- Qwen2.5-7B-Instruct (基座模型)

**训练配置**:
```python
MAX_SEQ_LENGTH = 2048
NUM_EPOCHS = 2
LEARNING_RATE = 1e-4
BATCH_SIZE = 4 (L4) / 8 (A100)
GRAD_ACCUM = 4
LORA_R = 64
LORA_ALPHA = 128
```

---

## 工作流组合

### 完整训练流程

```
Wwise 工程
    ↓ [XML to DSL 转译器]
原始 DSL
    ↓ [DSL 验证器]
验证通过的 DSL
    ↓ [指令生成器]
带指令的样本
    ↓ [样本裂变器]
增强后的数据集
    ↓ [数据集优化器]
平衡的数据集
    ↓ [数据集分析器]
质量报告
    ↓ [HuggingFace 上传器]
云端数据集
    ↓ [Colab 训练脚本]
GGUF 模型
    ↓ [Ollama]
本地部署
```

### 快速验证流程

```
DSL 代码
    ↓ [DSL 解析器]
AST
    ↓ [DSL 验证器]
验证报告
```

### 数据质量检查流程

```
JSONL 数据集
    ↓ [数据集分析器]
分析报告
    ↓ [数据集优化器]
优化后数据集
```

---

## 版本历史

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| V1.0 | 2024-12 | 初始版本，基础工具链 |
| V2.0 | 2024-12 | 添加 Attenuation 曲线支持 |
| V2.1 | 2024-12 | Unsloth 训练脚本优化 |

---

## 技能依赖

```
Python >= 3.10
├── lxml (XML 解析)
├── datasets (数据集处理)
├── transformers (模型加载)
├── unsloth (高效微调)
├── huggingface_hub (模型上传)
└── tqdm (进度条)
```
