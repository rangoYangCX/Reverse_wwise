# 🎵 NeuroWwise - Wwise DSL 逆向工程与模型训练工具链

> 从 Wwise 工程逆向生成 DSL 数据集,训练专业音频工程 AI 模型

## 📋 项目概述

NeuroWwise 是一套完整的工具链,用于:
1. 从 Wwise 工程文件逆向生成 DSL 代码
2. 构建高质量训练数据集
3. 微调 LLM 生成专业 Wwise DSL 代码

## 🏗️ 工具链架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        数据采集层                                │
├─────────────────────────────────────────────────────────────────┤
│  Wwise 工程 (.wwu)                                              │
│       ↓                                                         │
│  [XML to DSL 转译器] ──→ 原始 DSL 样本                          │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                        数据处理层                                │
├─────────────────────────────────────────────────────────────────┤
│  [DSL 解析器] ──→ 语法验证                                      │
│       ↓                                                         │
│  [DSL 验证器] ──→ 结构验证                                      │
│       ↓                                                         │
│  [指令生成器] ──→ 添加自然语言指令                              │
│       ↓                                                         │
│  [样本裂变器] ──→ 数据增强                                      │
│       ↓                                                         │
│  [数据集优化器] ──→ 平衡类型分布                                │
│       ↓                                                         │
│  [数据集分析器] ──→ 质量检查                                    │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                        模型训练层                                │
├─────────────────────────────────────────────────────────────────┤
│  [HuggingFace 上传] ──→ 数据集托管                              │
│       ↓                                                         │
│  [Colab 训练脚本] ──→ LoRA 微调 + GGUF 导出                     │
│       ↓                                                         │
│  Ollama 本地部署                                                │
└─────────────────────────────────────────────────────────────────┘
```

## 📦 工具清单

| # | 工具名称 | 版本 | 功能描述 |
|---|----------|------|----------|
| 1 | DSL 解析器 | V7.3 | 核心语法解析引擎,支持 Attenuation 曲线 |
| 2 | DSL 验证器 | V2.0 | 验证 DSL 结构完整性和语法正确性 |
| 3 | XML to DSL 转译器 | V3.4 | 将 Wwise .wwu 文件转换为 DSL 代码 |
| 4 | 指令生成器 | V1.1 | 为 DSL 样本生成自然语言指令 |
| 5 | 样本裂变器 | V1.1 | 数据增强,生成变体样本 |
| 6 | 数据集优化器 | V1.0 | 平衡样本类型分布 |
| 7 | 数据集分析器 | V1.0 | 分析数据集质量和分布 |
| 8 | HuggingFace 上传器 | V1.0 | 上传数据集到 HuggingFace |
| 9 | Colab 训练脚本 | V2.0 | Unsloth 微调 + GGUF 导出 |

## 🚀 快速开始

### 1. 环境准备

```bash
pip install -r requirements.txt
```

### 2. 完整工作流

```bash
# Step 1: 逆向 Wwise 工程
python xml_to_dsl_translator.py --input ./WwiseProject --output raw_samples.jsonl

# Step 2: 验证 DSL
python dsl_validator.py --input raw_samples.jsonl --output validated.jsonl

# Step 3: 生成指令
python instruction_generator.py --input validated.jsonl --output with_instructions.jsonl

# Step 4: 样本裂变
python sample_fission.py --input with_instructions.jsonl --output augmented.jsonl

# Step 5: 优化数据集
python dataset_optimizer.py --input augmented.jsonl --output optimized.jsonl

# Step 6: 分析数据集
python dataset_analyzer.py --input optimized.jsonl

# Step 7: 上传到 HuggingFace
python upload_to_hf.py optimized.jsonl

# Step 8: Colab 训练
# 在 Google Colab 中运行 cell1_unsloth.py 和 cell2_unsloth.py
```

## 📊 支持的 DSL 类型

| 类型 | 描述 | 示例 |
|------|------|------|
| Audio | 音频层级结构 | Container, Sound, RandomContainer |
| Attenuation | 衰减曲线 | 距离衰减、Spread、LPF |
| Event | 事件定义 | Play, Stop, Pause |
| SwitchGroup | 开关组 | 角色、地面类型 |
| StateGroup | 状态组 | 游戏状态、环境状态 |
| GameParameter | RTPC 参数 | 速度、距离、血量 |
| Workflow | 完整工作流 | Container + Event 组合 |

## 📁 项目结构

```
NeuroWwise/
├── README.md                    # 项目说明
├── SKILL.md                     # 技能定义文件
├── WORKFLOW.md                  # 工作流详细说明
├── requirements.txt             # Python 依赖
│
├── core/                        # 核心引擎
│   ├── dsl_parser.py           # DSL 解析器 V7.3
│   └── dsl_validator.py        # DSL 验证器 V2.0
│
├── reverse/                     # 逆向工程
│   └── xml_to_dsl.py           # XML 转 DSL V3.4
│
├── dataset/                     # 数据集处理
│   ├── instruction_generator.py # 指令生成器 V1.1
│   ├── sample_fission.py       # 样本裂变器 V1.1
│   ├── dataset_optimizer.py    # 数据集优化器 V1.0
│   ├── dataset_analyzer.py     # 数据集分析器 V1.0
│   └── upload_to_hf.py         # HuggingFace 上传
│
├── training/                    # 模型训练
│   └── train_unsloth_v51.py    # 一键训练脚本 V5.1
│
└── examples/                    # 示例文件
    ├── sample_dsl.txt          # DSL 示例
    └── sample_dataset.jsonl    # 数据集示例
```

## 🔧 配置说明

### 训练参数 (train_unsloth_v51.py)

```python
MAX_SEQ_LENGTH = 2048    # 最大序列长度
NUM_EPOCHS = 2           # 训练轮次
LEARNING_RATE = 1e-4     # 学习率
BATCH_SIZE = 4           # 批次大小 (L4)
GRADIENT_ACCUMULATION = 4 # 梯度累积
LORA_R = 64              # LoRA 秩
LORA_ALPHA = 128         # LoRA Alpha
```

### 数据集优化参数 (dataset_optimizer.py)

```python
TARGET_DISTRIBUTION = {
    "Audio": 0.25,
    "Attenuation": 0.15,
    "Event": 0.15,
    "SwitchGroup": 0.10,
    "StateGroup": 0.10,
    "GameParameter": 0.10,
    "Workflow": 0.15,
}
```

## 📈 训练统计

| 指标 | 值 |
|------|-----|
| 基座模型 | Qwen/Qwen2.5-Coder-7B-Instruct |
| 训练样本 | ~20,000 |
| 训练步数 | ~2,750 (2 epochs) |
| 训练时间 | ~3-4 小时 (L4 GPU) |
| 显存占用 | ~18 GB |
| 输出格式 | LoRA + GGUF (Q4_K_M) |

## 🖥️ 本地部署

```bash
# 下载模型
huggingface-cli download chengxuanyyy/Wwise-Engineering-Brain gguf/wwise-dsl-v2-Q4_K_M.gguf --local-dir ./
huggingface-cli download chengxuanyyy/Wwise-Engineering-Brain gguf/Modelfile --local-dir ./

# 创建 Ollama 模型
ollama create wwise-dsl -f Modelfile

# 运行
ollama run wwise-dsl
```

## 📄 许可证

MIT License

## 🙏 致谢

- [Unsloth](https://github.com/unslothai/unsloth) - 高效微调框架
- [Qwen](https://github.com/QwenLM/Qwen2.5) - 基座模型
- [Audiokinetic Wwise](https://www.audiokinetic.com/) - 音频引擎

---

*NeuroWwise - 让 AI 成为你的 Wwise 音频工程师*
